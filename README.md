# Visual Rhetoric Next.js App

This is a scaffolded Next.js (TypeScript) app created from the static `VisualRhetoricProject` site.

Quick start:

1. cd next-app
2. npm install
3. npm run dev

App will start at http://localhost:3000
