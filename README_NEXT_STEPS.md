Next steps after scaffold:

1. From `next-app` run `npm install` to install dependencies.
2. Run `npm run dev` to start the dev server at http://localhost:3000
3. Replace `public/*` placeholders with the real image and font binary files from the parent folder if desired.
4. Migrate additional HTML sections into `src/app/page.tsx` and port the existing `script.js` logic into React components/hooks.
